services:
  - type: web
    name: schoollms-backend
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: MONGO_URI
        fromGroup: schoollms-env
